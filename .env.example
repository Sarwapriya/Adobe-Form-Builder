# Base URL of the backend API (backend/), no trailing slash. The frontend calls
# `${VITE_API_BASE_URL}/api/v1/...` for every authenticated request.
# Vite only exposes env vars prefixed VITE_ to client code — see src/api/apiClient.ts.
VITE_API_BASE_URL=http://localhost:4000
